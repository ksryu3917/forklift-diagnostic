# V8.9.2 QUICK UI REDESIGN

목표: 현장에서 화면을 켠 뒤 5초 안에 '다음 한 점'을 알 수 있게 한다.

## 기본 진단 화면
- 기본모드는 빠른진단.
- 한 화면에 현재 필요한 점검 1개만 표시.
- 정상/이상 선택 후 adaptive branch 또는 순차진단이 다음 한 점으로 이동.
- 비adaptive 단순진단은 첫 FAIL에서 멈추고 해당 구간/다음 조치를 표시.
- 측정값/메모 입력칸은 기본화면에서 숨기고 `측정값 / 메모 기록`을 눌렀을 때만 표시.
- 작업세션/복사/초기화는 한 개 `작업기록` 메뉴로 축소.
- 시험조건/공구/OEM 근거/제한은 `시험조건 · OEM 근거 / 제한 보기`로 접음.

## 번호판등
- 첫 질문: 다른 미등/후미등 정상 여부.
- 다른 등이 정상이면 번호판등 소켓 +전원 → 접지 전압강하 → 전구/소켓 순.
- 다른 등도 함께 불량일 때만 공통 LAMP relay/light switch로 올라감.
- `다른 rear lamp 비교`를 회로의 SOURCE 부품처럼 표시하던 오류 제거.
- LAMP 퓨즈 정격은 매뉴얼 세대/사양 간 표기가 상충하므로 한 값으로 단정하지 않고 OEM_VERIFY로 표시.

## 전장 회로
- 번호판등/리어램프/경광등/아워미터/시트벨트의 generic `B+/IGN → 스위치/제어 → LOAD → GND` 도면 제거.
- 실제 기능 흐름/분기 이름으로 재작성.
- 측정포인트 화면의 generic SOURCE/CONTROL/HARNESS/LOAD 그림은 전장/에어컨에서 표시하지 않음.
- 전장 상세 화면의 실제 고장 전용 회로를 `위치 / 실제 회로 보기`로 연결.

## 화면 가독성
- Expert 268원인 화면: quick view + `바로 측정 시작`이 먼저. 도면/근거/분해조건은 상세 버튼 뒤로 이동.
- Electrical 38그래프: 현재 질문/선택지가 먼저. 회로/OEM 근거는 접기 상태가 기본.
- D24 engine 21그래프: 현재 질문/선택지가 먼저. 측정/센서 위치 버튼 다음, 회로/공구/OEM은 상세 버튼 뒤.

## 확대
- OEM 원본, 재작성 회로, 위치맵, 센서맵, 부품도 전부 공통 확대뷰 사용.
- pinch zoom / drag pan / double tap zoom-reset.

## Release
- versionCode 36
- versionName 0.18-rc-expert-v8.9.2-quick-ui
- state RC EXPERT V8.9.2 QUICK UI
