package com.ryu.forkliftdiagnostic;import android.app.*;import android.os.*;import android.graphics.*;import android.graphics.drawable.*;import android.view.*;import android.widget.*;import org.json.*;import java.io.*;import java.nio.charset.StandardCharsets;import java.util.*;public class ExpertDiagnosticActivity extends Activity {    private LinearLayout body;    private JSONObject root,item;    private final int NAVY=Color.rgb(16,38,58), BLUE=Color.rgb(17,117,214), BG=Color.rgb(242,245,247);    @Override public void onCreate(Bundle b){        super.onCreate(b);        try{            root=new JSONObject(readAsset("expert_diag_v2.json"));            String cid=getIntent().getStringExtra("cause_id");            item=findItem(cid);            if(item==null) throw new Exception("원인 진단 데이터가 없습니다: "+cid);            render();        }catch(Exception e){fatal("전문가 진단 로딩 오류: "+e.getMessage());}    }    private String readAsset(String name)throws Exception{        InputStream is=getAssets().open(name); ByteArrayOutputStream os=new ByteArrayOutputStream();        byte[] buf=new byte[8192]; int n; while((n=is.read(buf))>0) os.write(buf,0,n); is.close();        return os.toString(StandardCharsets.UTF_8.name());    }    private JSONObject findItem(String id)throws Exception{        JSONArray a=root.getJSONArray("items");
