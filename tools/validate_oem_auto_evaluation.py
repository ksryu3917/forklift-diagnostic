#!/usr/bin/env python3from pathlib import Pathimport json,sys,math,reROOT=Path(sys.argv[1]).resolve() if len(sys.argv)>1 else Path(__file__).resolve().parents[1]A=ROOT/'app/src/main/assets/test_point_locator_v1.json'J=ROOT/'app/src/main/java/com/ryu/forkliftdiagnostic/TestPointLocatorActivity.java'R=ROOT/'build/reports/oem_auto_eval_v88.md'errors=[];warnings=[]d=json.loads(A.read_text(encoding='utf-8'));js=J.read_text(encoding='utf-8')allowed={'range','max','min'}; auto=[]; profiles_count=0; scenarios=0required={('TM_PRESSURE_TAPS',x) for x in ['TM2','TM3','TM4','TM5','TM6','TM7']}|{('HYD_MAIN_RELIEF',x) for x in ['HY1','HY2','HY3','HY4']}|{('WORK_EQUIPMENT_CONTROL',x) for x in ['WK2','WK3']}|{('MAST_CYLINDER_DIAG',x) for x in ['MS3','MS6']}|{('EL_FR_CONTROL','FR5')}seen=set()def verdict(kind,pr,v):    if kind=='range':return pr['min']<=v<=pr['max']    if kind=='max':return v<=pr['max']    if kind=='min':return v>=pr['min']    raise ValueError(kind)def scenario_values(kind,pr):    if kind=='range':        lo,hi=float(pr['min']),float(pr['max']); span=max(abs(hi-lo),max(abs(lo),abs(hi),1.0)*0.01)        vals=[lo-span,lo-span*.1,lo,lo+span*.01,(lo+hi)/2,hi-span*.01,hi,hi+span*.1,hi+span]        # pad with deterministic values across / outside range until 30        for i in range(21): vals.append(lo-span+(hi-lo+2*span)*i/20)        return vals[:30]    if kind=='max':        mx=float(pr['max']); span=max(abs(mx)*0.1,0.1);vals=[mx-span,mx-span*.1,mx,mx+span*.1,mx+span]        vals += [mx-1.5*span+3*span*i/24 for i in range(25)]        return vals[:30]    mn=float(pr['min']);span=max(abs(mn)*0.1,0.1);vals=[mn-span,mn-span*.1,mn,mn+span*.1,mn+span]    vals += [mn-1.5*span+3*span*i/24 for i in range(25)]    return vals[:30]for gid,g in d.get('groups',{}).items():    for p in g.get('points',[]):        ae=p.get('auto_eval')        if not ae:continue        seen.add((gid,p.get('id')));auto.append((gid,p))        if p.get('source_class')!='OEM_EXACT':errors.append(f'{gid}/{p.get("id")}: auto_eval on non-OEM_EXACT source {p.get("source_class")}')        if ae.get('authority')!='OEM_EXACT_ONLY':errors.append(f'{gid}/{p.get("id")}: authority must be OEM_EXACT_ONLY')        if '약' in p.get('expected','') or 'approx' in p.get('expected','').lower():errors.append(f'{gid}/{p.get("id")}: approximate spec must not auto-evaluate')        kind=ae.get('kind');        if kind not in allowed:errors.append(f'{gid}/{p.get("id")}: invalid kind {kind}');continue        profiles=ae.get('profiles') or [];units=ae.get('units') or []        if not profiles:errors.append(f'{gid}/{p.get("id")}: no profiles')        if not units:errors.append(f'{gid}/{p.get("id")}: no units')        for u in units:            if not u.get('unit'):errors.append(f'{gid}/{p.get("id")}: blank unit')            if not isinstance(u.get('to_base'),(int,float)) or u.get('to_base',0)<=0:errors.append(f'{gid}/{p.get("id")}: invalid conversion {u}')        for pr in profiles:            profiles_count+=1            if kind=='range' and (not isinstance(pr.get('min'),(int,float)) or not isinstance(pr.get('max'),(int,float)) or pr['min']>pr['max']):errors.append(f'{gid}/{p.get("id")}/{pr.get("id")}: invalid range')            if kind=='max' and not isinstance(pr.get('max'),(int,float)):errors.append(f'{gid}/{p.get("id")}/{pr.get("id")}: invalid max')            if kind=='min' and not isinstance(pr.get('min'),(int,float)):errors.append(f'{gid}/{p.get("id")}/{pr.get("id")}: invalid min')            try:
